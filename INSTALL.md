# Installation Guide

This guide explains how to install and use the VS Code extension from a `.vsix` file.

---

## 📦 Install from VSIX (Recommended)

### 1. Open VS Code
Launch **Visual Studio Code** on your machine.

### 2. Open Command Palette
Press: Ctrl + Shift + P

### 3. Run Install Command
Type and select:

### 4. Select the Extension File
Navigate to the `installation/` folder in this project and select the `.vsix` file you want to install.

### 5. Reload VS Code
If prompted, click **Reload** to activate the extension.

---

## 💻 Install via Terminal (Alternative)

You can also install the extension using the VS Code CLI:

```bash
code --install-extension path/to/your-extension.vsix
